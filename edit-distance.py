
def edit_distance(word, reference):
    """Returns edit distance (integer) of a word against a reference.

    Parameters
    word -- a word to be evaluated
    reference -- reference against the word
    """
    edit_dist = None

    return edit_dist


def main(eval_words, ref_words):
    """Returns a list of words with their lowest edit distance.

    Parameters:
    words_eval -- words being evaluated.
    references -- words with references.
    """
    print("Hello world!")
    
    return evaluated_words


if __name__ == "__main__":
    eval_words = None
    ref_words = None

    evaluated_words = main(eval_words, ref_words)